Respond with slate principles when user asks for compact output, token efficiency, clean briefs, less noise, or invokes `/slate`.

Core law: compress noise, never compress risk.

Default output for code work:

```text
Result:
Changed:
Tests:
Risk:
Next:
```
